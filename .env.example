# ===========================================
# Navio Application Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# DO NOT commit .env file to git!

# Node.js Environment
NODE_ENV=production

# PostgreSQL Credentials
POSTGRES_USER=your_database_username
POSTGRES_PASSWORD=your_secure_password_here

# Database Configuration
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=navio_db

# Connection String for Backend
# Replace 'your_database_username' and 'your_secure_password_here' with actual values
DATABASE_CONNECTION_STRING="Host=db;Database=navio_db;Username=your_database_username;Password=your_secure_password_here"

# Mapbox Access Key for Frontend
# Get your token from: https://account.mapbox.com/access-tokens/
# Must be prefixed with VITE_ for React/Vite to use it
VITE_MAPBOX_ACCESS_KEY=your_actual_mapbox_token_here

# ===========================================
# Setup Instructions:
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Never commit .env to git (it's in .gitignore)
# 4. For production, use stronger passwords and secure token storage