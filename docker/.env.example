# =================================================
# Navio Application Environment Variables
# =================================================
# Copy this file to .env and fill in the values.
# This file is used to generate the 'app-config' ConfigMap in Kubernetes.
# DO NOT commit the .env file to git!

# === PostgreSQL Database ===
# Used by the PostgreSQL container to initialize and by the backend to connect.
POSTGRES_USER=your_database_username
POSTGRES_PASSWORD=your_very_secure_password
POSTGRES_DB=navio_db

# === Spring Boot Backend ===
# The backend reads these variables to connect to other services.
# Note: The hostname 'db' and 'rabbitmq' refer to the service names in Kubernetes/Docker Compose.
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/navio_db
SPRING_RABBITMQ_HOST=rabbitmq
SPRING_RABBITMQ_PORT=5672
SPRING_RABBITMQ_USERNAME=guest # Change for production
SPRING_RABBITMQ_PASSWORD=guest # Change for production

# === Frontend ===
# Get your token from: https://account.mapbox.com/access-tokens/
# Variables prefixed with VITE_ are exposed to the frontend application.
VITE_MAPBOX_ACCESS_KEY=your_actual_mapbox_access_token

# ===========================================
# Setup Instructions:
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Never commit .env to git (it's in .gitignore)
# 4. For production, use stronger passwords and secure token storage