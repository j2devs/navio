import React from "react";

const DiscoverGrid: React.FC = () => {
    return (<section id="discover-section" className="grid gap-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
        {/* Empty discover section for now */}
    </section>);
};

export default React.memo(DiscoverGrid);
